server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/shop_shoe_superteam?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&createDatabaseIfnNotExist=true}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:root}
#    url: jdbc:mysql://206.189.42.99/shop_shoe_superteam?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
#    username: root
    driverClassName: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
    signerKey: "1cLc5GX0Em888Eol5qUD7MmqprRtdyV7mFEBWftpuHDlmdbSZdXrTa74uv5q9/rD"
    validDuration: 3600
    refreshableDuration: 36000


vnPay:
  version: ${VNP_VERSION:2.1.0}
  command: ${VNP_COMMAND:pay}
  orderType: ${VNP_ORDER_TYPE:other}
  currCode: ${VNP_CURR_CODE:VND}
  hash-secret: ${VNP_HASH_SECRET:3JMZVAOGMYMN0OR2B0LHODUA0IN3OF0B}
  tmn-code: ${VNP_TMN_CODE:SOHPW8FT}
  payment-url: ${VNP_PAYMENT_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
  return-url: ${VNP_RETURN_URL:https://unpatrician-underogatively-bronson.ngrok-free.dev/checkout/payment-callback}
  # Debug / compatibility flags (try variations if signature fails):
  # When true, values will be percent-encoded during HMAC calculation (some sandboxes expect this)
  hash-encode: ${VNP_HASH_ENCODE:true}
  # When true, HMAC hex will be converted to uppercase before sending to VNPay
  hash-upper: ${VNP_HASH_UPPER:true}
  # When true, add &vnp_SecureHashType=SHA512 to the payment URL
  send-hash-type: ${VNP_SEND_HASH_TYPE:true}
  demo: ${VNPAY_DEMO:false}
  timezone: ${VNPAY_TIMEZONE:GMT+7}

mail:
  apiKey: ""

ai:
  openai:
    endpoint: ${AI_OPENAI_ENDPOINT:https://api.openai.com/v1/chat/completions}
    api-key: ${AI_OPENAI_APIKEY}
